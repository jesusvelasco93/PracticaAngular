angular.module("goldencrew").controller("AppController",
    ["$scope", "$location", function ($scope, $location){

        // Model init
        $scope.model = {
            title: "Funciona"
        };
    }]
);